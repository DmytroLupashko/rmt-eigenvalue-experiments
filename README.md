# The largest eigenvalue of an adjacency matrix of a
random graph

### Ways to continue our work
- Consider all eigenvalues and find their distribution using discrete measure
- Find normalized distribution of lambda 1 as random variable
- Test whether result depends on elements of matrix A, if replace normalized Bernoulli variable with the same parameters (E = 0, Var = 1, Var(X^4) < inf) 